#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   int a = 238903;
   int b = 2;
   if(a%b == 0)
   {printf("a is even");
   }
   else if (a%b!=0)
   {printf("a is odd");
   }
}
